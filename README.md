# 减脂备餐

个人减脂备餐菜谱、周菜单、采购单。配套网页：`index.html`（纯静态，直接读取本目录里的 Markdown 文件渲染，没有构建步骤，也没有第二份内容副本）。

- `食谱/` —— 每道菜一份笔记：视频链接、份量、热量蛋白、用具、步骤
- `周菜单/` —— 每周一份，安排哪天吃哪道
- `采购单/` —— 对应周菜单的采购清单，可在网页上勾选
- `data/manifest.json` —— 网页用来找到上面这些文件的清单（不是内容副本）

本仓库只发布备餐相关内容，不含个人健康/日程信息。
